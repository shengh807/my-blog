{
  "name": "my-blog",
  "version": "1.0.0",
  "private": true,
  "description": "마크다운으로 글을 쓰면 정적 블로그로 빌드되는 나만의 블로그",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "new": "node scripts/new-post.js",
    "preview": "npm run build && node scripts/serve.js"
  },
  "dependencies": {}
}
